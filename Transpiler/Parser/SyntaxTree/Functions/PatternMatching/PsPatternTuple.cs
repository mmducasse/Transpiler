namespace Transpiler.Parse
{
    //public record PatternTupleNode(IReadOnlyList<IPatternNode> Members) : IPatternNode
    //{
    //    public string Print(int i)
    //    {
    //        return Members.Select(m => m.Print(i)).Separate(", ");
    //    }
    //}
}
