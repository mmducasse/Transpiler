
TODO:

- Add TypeParameters to the types of functions that get determined during Analysis
- Clean up generator, maybe eliminate redundant asts
- Prohibit accessors for union data types

- Add vars in REPL
- Tail call recursion optimization
- Make private indicators do something
- Explict type annotations inline
- Operator associativity
- $0, $1, etc to represent lambda inputs
- Type aliases
- Pattern aliases
- Nested unions
- Arbitrary unions
- Precicates in match cases after 'if'

